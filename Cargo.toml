[package]
name = "mdbook-icons"
version = "0.1.0"
edition = "2024"

[dependencies]
mdbook = "0.4"
pulldown-cmark = "0.10"
regex = "1"
serde = { version = "1", features = ["derive"] }
serde_json = "1"
urlencoding = "2"
ureq = { version = "2", default-features = false, features = ["tls"] }
